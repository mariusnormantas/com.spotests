/** @format */

import React from "react";

// Tabs panel component's props.
export type TabsPanelProps = {
  value?: string;
  keepMounted?: boolean;
  withRouter?: boolean;
  children: React.ReactNode;
};
